
\documentclass[9pt,onecolumn,twoside]{preprint}
% Use the documentclass option 'lineno' to view line numbers
\setlength{\marginparwidth}{2cm}
\usepackage{subcaption}
\usepackage{float}
\usepackage{pgf}
\providecommand{\mathdefault}[1]{#1}

\title{Title}

\author[$\ast$,1]{First Author}

\affil[$\ast$]{Affiliation}

\keywords{Keyword one, keyword two}

\runningtitle{Running title} % For use in the footer
\runningauthor{Author \textit{et al.}}

%%% Abstract %%%%%%%%%%%%%%%%%%
\begin{abstract}
Abstract text goes here.
\end{abstract}
%%%%%%%%%%%%%%%%%%%%%%%%%%

\setboolean{displaycopyright}{true}

\begin{document}

\maketitle
\thispagestyle{firststyle}

\setboolean{displaylineno}{true}
\ifthenelse{\boolean{displaylineno}}{\linenumbers}{}

\section{Introduction}


The effective population size of a population, $N_e$, is a central parameter in evolutionary biology and population genetics. There is a long tradition of studying temporal changes in $N_e$ over large time scales to understand the evolutionary history of a population. More recently, $N_e$ has been proposed as a key indicator in conservation biology by different organisations \citep{van_Oosterhout_2026}. A reduced $N_e$ decreases the efficiency of natural selection and increases the strength of genetic drift. As a consequence, estimating $N_e$ is a crucial component to predict a population's ability to persist and thrive in the Anthropocene.

In an applied conservation context, we are interested in estimating $N_e$ for very recent generations. However, obtaining reliable estimates of very recent $N_e$ (say, < 100 generations in the past) is challenging. For example, estimates based solely on genetic diversity (also known as sample heterozygosity), although widely used, are highly limited for this application as they are largely shaped by the $N_e$ of the ancestral population many thousands of generations in the past \citep{van_Oosterhout_2026}. Other popular methods like PSMC \citep{Liu_2016}, MSMC2 \citep{Schiffels_2020} or Bayesian Skyline Plots \citep{Drummond_2005} also provide limited resolution at these time scales \citep{van_Oosterhout_2026}.

Generally, we expect methods that depend on site-summary statistics to struggle resolving recent demography as they depend on mutations, which accumulate slowly. As a result, there is often a pronounced delay, or time-lag \citep{Gargiulo_2025}, between the time of a major demographic disturbance and the time when we can observe notable changes in site statistics (see later XXX).

Alternatively, we may use the signal of recombination events during meiosis. Because recombination events happen much more frequently, they provide a more informative signal to study very recent $N_e$. One could then expect methods that (approximately) model genome sequences under the coalescence with recombination (such as PSMC or MSMC2) to infer accurately recent (as well as ancient) demography. In this case, however, they are limited by the sample sizes for which inference remains tractable. Recent pairwise coalescence events of a handful of chromosomes are rare, which therefore imposes limits on the time-scale resolution of these methods.

Provided that one has access to a genetic map, alternative summary statistics of genomic variation have been proven successful. Namely, identity-by-descent sharing (IBDNe \citep{Browning_2015}, HapNe-IBD \citep{Fournier_2023}) and linkage disequilibrium (GONE \citep{Santiago_2020}, HapNe-LD \citep{Fournier_2023}). These methods target these summary statistics because they are almost exclusively shaped by recent recombination events, which makes them an interesting target of inference. However, they are not informative of ancient demography.

In this work, we propose a model-based Bayesian inference framework for inferring recent $N_e(t)$ from unphased genomic data. We make two major contributions. First, we investigate to what extent we can exploit the proxy between recent shared ancestry and long-range linkage disequilibrium proposed by \citet{Fournier_2023} outside non-human populations. Second, we introduce a a novel inference method that jointly models mean sample heterozygosity and linkage disequilibrium decay to fit parametric demographic models, combining analytical and Monte Carlo predictions.

\section{Results}

\subsection{Choice of informative summary statistics}

The genetic diversity, $\pi$ is a classical genetic summary statistic. It's expected value, $\mathbb{E}[\pi]$, is defined as the probability that a pair of randomly chosen chromosomes differ at a given nucleotide site. It is dominated by the $N_e$ of the ancestral population many thousands of generations ago. As a result, it is weakly informative of recent $N_e$, and we must wait a substantial amount of time in order to observe appreciable changes in $\mathbb{E}[\pi]$. This is an alternative interpretation of a phenomenon known as genetic drift debt \citep{Gargiulo_2025}.

On the contrary, measures of linkage disequilibrium are more sensitive to recent changes in $N_e$ and are a more powerful target for inference of recent demographic history. Let $G_{x,i}$ denote the genotype (allele count, $\{0, 1\}$ if haploid or $\{0, 1, 2\}$ if diploid) at locus $x$ in individual $i$. Here, we focus on the product of centered and standardized genotypes at two bi-allelic loci

\begin{equation}
	X_i X_j Y_i Y_j,
	\quad \text{where loci } x \text{ and } y \text{ are } u \text{ Morgan apart},
\end{equation}

where $X_i$ and $Y_i$ corresponds to $G_{x,i}$ and $G_{y,i}$ transformed such the resulting random variable has mean zero and unit variance (see Appendix XXX).

\textcolor{red}{Maybe it would be less confusing to just say in the main text we assume HW and allele frequencies}

Empirically, we can estimate it by averaging across several pairs of individuals and across many pairs of loci $x, y$ whose distance falls in a given bin $u\in[l, r]$. We denote such an estimator as $\overline{X_i X_j Y_i Y_j}$ and, under mild assumptions (see Appendix XXX), it converges to $\mathbb E [	X_i X_j Y_i Y_j]$ as the number of pairs of loci and individuals grows.

\subsection{Case study: a very recent catastrophic disturbance}

To illustrate how these summary statistics are informative at different temporal scales, we study the impact of a strong recent bottleneck in a population. Consider an instantaneous bottleneck that happened as recently as five generations ago that results in a tenfold decrease in current $Ne$ (Figure \ref{fig:conceptual:demography}). In a conservation setting, this model mimics the effect of some catastrophic disturbance.

%% Mention 500 is the limit for endanger

Compared to some reference values (a population in which such bottlenecks did not occur), we predict a decrease in genetic diversity. However, the observed simulated loss in diversity (Figure \ref{fig:conceptual:pi_hist}) is relatively very small and would be difficult to detect empirically. Likewise, we predict an increase in $\mathbb E[X_iX_jY_iY_j]$ (as well as other measures of linkage disequilibrium) because in a smaller population, we expect fewer recombination events to break apart the ancestral haplotypes shared by any pair of chromosomes.

$\overline{X_iX_jY_iY_j}$ is a more informative summary statistic in the sense that we expect a higher relative change (Figure \ref{fig:conceptual:ld}) between both scenarios. In practice, we measure $\overline{X_iX_jY_iY_j}$ across pairs of loci at different binning distances. As genetic distance increases, $\mathbb{E}[X_iX_jY_iY_j]$ must decrease as there are more opportunities for recombination to occur between them. The rate at which $\mathbb{E}[X_iX_jY_iY_j]$ is, itself, informative of recent demography.

\begin{figure}[h]
	\centering
	% --- Row 1: a, ba, bb ---
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\resizebox{\linewidth}{!}{\input{figures/conceptual/conceptual_figure_panel_a.pgf}}
		\caption{Reference and bottleneck demography.}
		\label{fig:conceptual:demography}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\resizebox{\linewidth}{!}{\input{figures/conceptual/conceptual_figure_panel_ba.pgf}}
		\caption{Distribution of per-window $\overline{\pi}$.}
		\label{fig:conceptual:pi_hist}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\resizebox{\linewidth}{!}{\input{figures/conceptual/conceptual_figure_panel_bb.pgf}}
		\caption{Distribution of per-window $\overline{X_iX_jY_iY_j}$}
		\label{fig:conceptual:ld}
	\end{subfigure}
	\caption{Demography and per-window summary statistics under a constant $N_e$ and a recent bottleneck. Panels \ref{fig:conceptual:pi_hist} and \ref{fig:conceptual:ld} show distributions across 50 independent windows of 20~cM from 50 diploid individuals simulated with \texttt{msprime}. \ref{fig:conceptual:ld} shows jittered replicates for the different genetic distances. }
	\label{fig:conceptual}
\end{figure}

\begin{figure}[h]
	\centering
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\resizebox{\linewidth}{!}{\input{figures/conceptual/conceptual_figure_panel_c.pgf}}
		\caption{Relative change in nucleotide diversity ($\pi$) as a function of bottleneck start time.}
		\label{fig:conceptual:pi_change}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\resizebox{\linewidth}{!}{\input{figures/conceptual/conceptual_figure_panel_d.pgf}}
		\caption{Relative change in LD at the shortest distance bin as a function of bottleneck start time.}
		\label{fig:conceptual:ld_change}
	\end{subfigure}
	\caption{Relative change in summary statistics as a function of bottleneck start time. Dots are means and error bars are standard errors across 50 independent 20~cM windows from 50 diploid individuals simulated with \texttt{msprime}.}
	\label{fig:conceptual_change}
\end{figure}


In Figure \ref{fig:conceptual:pi_change} and Figure \ref{fig:conceptual:pi_change} we further analyze the simulated relative change of $\overline{\pi}$ and $\overline{X_iX_jY_iY_j}$ as a function of the bottleneck start time between 1 and 100. Note that no summary statistic can detect a demographic change that happened zero generations ago. In this scenario, the simulated relatives’ changes, compared to the reference, are one order of magnitude larger for $\overline{X_iX_jY_iY_j}$ compared to those from $\overline{\pi}$, making this summary statistic a much more informative inference target.

Note, however, that in practice the temporal scale at which the decay in $\overline{X_iX_jY_iY_j}$ is informative is limited by the actual chromosome size and recombination rates. We found, however, that for realistic values across many species $\overline{X_iX_jY_iY_j}$ is informative of recent demography, making it a promising summary statistic in conservation genomics.

\subsection{Analytical predictions}

Predictions of informative summary statistics are what allow us to confront demographic models with genomic data. Since full-genome probabilistic models are intractable, we rely on the derived expectations $\mathbb E[\pi]$ and $\mathbb E[X_iX_jY_iY_j]$, which we then compare against mean values estimated over large windows across thousands of loci. Throughout, we consider a panmictic population whose effective size follows a parametric trajectory $N_e(t \mid \theta)$ (e.g.\ constant, piecewise-constant, or piecewise-exponential).

In coalescence of coalescence theory, $N_e$ is defined in terms of the inverse of the instantaneous coalescence rate XXX, that we denote as $\lambda(t)$. For a diploid population:

\begin{equation}
	\lambda(t \mid \theta) = \frac{1}{2N_e(t \mid \theta)}
\end{equation}

It is well known that, for a panmictic population of constant size $N$, $\mathbb{E}[\pi] = 4\mu N$, where $\mu$ is the mutation rate XXX. More generally,

\begin{equation}
	\mathbb{E}[\pi \mid \theta] = 2\mu \int_0^\infty t\, f(t \mid \theta)\, dt,
	\label{eq:pi}
\end{equation}

where $f(t \mid \theta)$ is the coalescence density (i.e.\ the probability density of two lineages finding their most recent common ancestor $t$ generations ago). At any given time, this density factorises into the probability of not having yet coalesced times the instantaneous coalescence rate so that

\begin{equation}
	f(t \mid \theta) = \lambda(t \mid \theta)\, \exp\!\left(-\int_0^t \lambda(s \mid \theta)\, ds\right).
	\label{eq:tmrca}
\end{equation}

Unbiased predictions of $\mathbb{E}[\pi \mid \theta]$ can be obtained by solving Equation \ref{eq:tmrca} for different forms of $N_e(t)$ trajectories and plugging them back in Equation \ref{eq:pi}. From Equation \ref{eq:pi} it is also apparent why the $\pi$ is weakly informative of recent $N_e$, as it's value will most likely dominated by the long tail in the integral.

Unfortunately, no analogous closed form is known for $\mathbb E[X_iX_jY_iY_j \mid \theta]$. Nevertheless, one can calculate an unbiased Montecarlo estimator by averaging over $M$ independent coalescent simulations simulations under the demographic model $N_e(t \mid \theta)$. In each of $M$ replicates we simulate a pair of loci $u$ Morgans apart in a sample of $n$ individuals.
\begin{equation}
	\widehat{\mathbb E}_{\mathrm{MC}}[X_iX_jY_iY_j \mid \theta] = \frac{1}{M} \sum_{m=1}^{M} \binom{n}{2}^{-1} \sum_{i<j} X_i^{(m)} X_j^{(m)} Y_i^{(m)} Y_j^{(m)},
	\label{eq:mc_ld}
\end{equation}
where $X_i^{(m)}, Y_i^{(m)}$ are the centred and standardised genotypes of individual $i$ at the two loci in replicate $m$.

Although unbiased, the Montecarlo estimator is computationally expensive. \citet{Fournier_2023} found the approximation expression and made an elegant link between long-range linkage-disequilibrium and coalescence with recombination. They suggested that pairs of high-frequency loci that are not very close ($u > 0.5 cM$)  satisfy

\begin{equation}
	\mathbb E[X_iX_jY_iY_j \mid \theta] \approx \widehat{\mathbb E}_{\mathrm{F}}[X_iX_jY_iY_j \mid \theta] =  S(u \mid \theta),
	\label{eq:fournier_ld}
\end{equation}

where $S(u)$ is the probability that in pair of randomly chosen chromosomes two loci loci that are $u$ Morgans apart are in identity-by-descent (i.e. they have not been broken apart by recombination since their most recent common ancestor (MRCA)). They obtained identical approximations for the haploid and diploid case.

Under the Sequentially Markovian coalescent approximation, one can further simplify Equation \ref{eq:fournier_ld} and to obtain

\begin{equation}
	\widehat{\mathbb E}_{\mathrm{F}}[X_iX_jY_iY_j \mid \theta] \stackrel{\text{by SMC}}{\approx} \int_0^\infty \exp(-2tu)\, f(t \mid \theta)\, dt
	\label{eq:smc}
\end{equation}

that is, the probability that no recombination event has happened since their MRCA.

Comparing Equation \ref{eq:smc} and Equation \ref{eq:pi} provides insight on why $\mathbb E[X_iX_jY_iY_j]$ is a more informative summary statistic of recent demography than $\mathbb E[\pi]$. A possible interpretation is that Equation \ref{eq:pi} assigns weights that increase linearly with time whereas in Equation \ref{eq:smc} decay exponentially with time. Therefore, although both expression depend on $f(t \mid \theta)$, $\mathbb E[X_iX_jY_iY_j]$ will be often dominated by $N_e$ in very recent generations.

\subsection{Prediction error}

The estimator proposed by \citet{Fournier_2023} is surprisingly elegant. However, their application seemed to be limited to human populations. Next, we evaluate the bias introduced by the approximation in a broader range of species and demographic models.

First, we simulated full-genome genomic variation data for different species models in the stdpopsim's catalogue \citep{Gower_2025}. The goal was to test the predictions in realistic parameters regime. Results are shown in Figure \ref{fig:stdpopsim}. Overall, we found $\widehat{\mathbb E}_{\mathrm{F}}[X_iX_jY_iY_j]$ to match well simulated summary statistic across various sampling configurations, demographic histories and recombination rates ($10^{-11}$ to $10^{-8}$ per base pair per generation). We observed a more noticeable bias for the model of \textit{Caenorhabditis elegans} (Figure \ref{fig:stdpopsim:caeele}), whose extremely low recombination rate yields chromosomes whose length is the order of the minimum genetic distance of 0.5cM  suggested by \citet{Fournier_2023}.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/stdpopsim/stdpopsim_holsteinfriesian.pdf}
		\caption{Holstein-Friesian cattle (\textit{Bos taurus}), HolsteinFriesian\_1M13.}
		\label{fig:stdpopsim:holsteinfriesian}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/stdpopsim/stdpopsim_vaquita.pdf}
		\caption{Vaquita (\textit{Phocoena sinus}), Vaquita2Epoch\_1R22.}
		\label{fig:stdpopsim:vaquita}
	\end{subfigure}

	\vspace{0.5em}

	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/stdpopsim/stdpopsim_orysat.pdf}
		\caption{\textit{Oryza sativa}, constant $N_e$.}
		\label{fig:stdpopsim:orysat}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/stdpopsim/stdpopsim_drosec.pdf}
		\caption{\textit{Drosophila sechellia}, constant $N_e$.}
		\label{fig:stdpopsim:drosec}
	\end{subfigure}
	\caption{Deterministic $\widehat{\mathbb E}_{\mathrm{F}}[X_iX_jY_iY_j]$ predictions vs.\ \texttt{stdpopsim} simulations. Simulations were generated with \texttt{msprime} using a 100-generation discrete-time Wright-Fisher phase followed by the standard coalescent. Solid lines and shaded areas correspond to the observed mean and its 95\% bootstrap confidence interval. Dashed lines correspond to predictions under the SMC$'$ approximation using the true demographic parameters of each simulation.}
	\label{fig:stdpopsim}
\end{figure}




%% STOP READING
\newpage

They suggested that pairs of high-frequency loci (with minor allele frequency greater than 0.25) that are not very close ($u > 0.5 cM$) satisfy that $\mathbb{ E}[X_iX_j \mid\text{IBD}] \approx \mathbb{ E}[X^2] = 1$ and $\mathbb{ E}[X_iX_j \mid\neg\text{IBD}] \approx \mathbb{ E}[X]^2 = 0$. Under these assumptions, it follows that

\begin{align}
	\mathbb{E}[X_{i}Y_{i}X_{j}Y_{j}]
	 & = S(u)\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \text{IBD}]
	+ (1 - S(u))\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \neg\text{IBD}] = S(u)
\end{align}



Although unbiased, this estimator is expensive. \citet{Fournier_2023} found the approximation expression and made an elegant link between long-range LD and IBD-sharing. They suggested that pairs of high-frequency loci (with minor allele frequency greater than 0.25) that are not very close ($u > 0.5 cM$) satisfy that $\mathbb{ E}[X_iX_j \mid\text{IBD}] \approx \mathbb{ E}[X^2] = 1$ and $\mathbb{ E}[X_iX_j \mid\neg\text{IBD}] \approx \mathbb{ E}[X]^2 = 0$. Under these assumptions, it follows that

\begin{align}
	\mathbb{E}[X_{i}Y_{i}X_{j}Y_{j}]
	 & = S(u)\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \text{IBD}]
	+ (1 - S(u))\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \neg\text{IBD}] = S(u)
\end{align}

where $S(u)$ is the probability that two loci that are $u$ Morgans apart are in IBD. For the case of diploid (unphased) individuals, \citet{Fournier_2023} proposed to neglect correlations induced by more than one pair of haplotypes being in IBD and suggested that
\begin{align}
	\mathbb{E}[G_{x,i} G_{y,i} G_{x,j} G_{y,j}]
	 & = \mathbb{E}[(X_{i,1}+X_{i,2})(Y_{i,1}+Y_{i,2})(X_{j,1}+X_{j,2})(Y_{j,1}+Y_{j,2})] \\
	 & = \frac{1}{4} \sum_{\alpha,\beta,\gamma,\delta = \{1,2\}}
	\mathbb{E}[X_{i,\alpha} X_{j,\beta} Y_{i,\gamma} Y_{j,\delta}]                        \\
	 & \approx \frac{1}{4} \sum_{\alpha,\beta = \{1,2\}}
	\mathbb{E}[X_{i,\alpha} X_{j,\beta} Y_{i,\alpha} Y_{j,\beta}]                         \\
	 & \approx \frac{1}{4} \sum_{\alpha,\beta = \{1,2\}} S(u) = S(u)
\end{align}

where $G_{x,i}$ and $G_{y,i}$ are the centred and standardised unphased diploid genotypes.

\citet{Fournier_2023}'s proxy, although approximate, gives an intuition of why linkage disequilibrium is informative of recent demography, as, essentially:

\begin{equation}
	\mathbb E[\mathbb E[X_iX_jY_iY_j]] \approx \int_0^\infty \exp(-2ty) f(t)\,dt
\end{equation}

which can be interpreted as assigning weights to each time in the past that decays exponentially.

Let us consider a set of bi-allelic loci scattered across the genome in a diploid organism. As usual, let's consider that we have access to the states $\{0, 1, 2\}$ of each locus for a set of individuals.

The genetic diversity or sample heterozygosity,  It is defined as XXX. The genome-wide average genetic diversity within the sample set is directly related to $Ne$, as decreases (or increases) in $Ne$ result in a decrease (or increase) in genetic diversity.

$\mathbb E[X_iX_jY_iY_j]$ is a measure of linkage disequilibrium and it measures the correlation between loci that are $u$ units apart. A pair of neutral loci that are physically in the same chromosome experience some non-zero amount of correlation which decays with distance (more opportunities to be separated during meiosis by a recombination event). However, it also depends on $Ne$. This is because higher population sizes result in a longer time to the most recent common ancestors which also results in more recombination events. Therefore, $\mathbb E[X_iX_jY_iY_j]$ is related with $Ne$, as decreases (or increases) in $Ne$ result in an increase (or decrease) in $\mathbb E[X_iX_jY_iY_j]$.

Although the sample heterozygosity is informative of $Ne$, it responds slowly. This is a phenomenon known as genetic drift debt \citep{Gargiulo_2025}. Instead, $\mathbb E[X_iX_jY_iY_j]$ is more informative of very recent $Ne$. As an extreme example, let's study the impact of a strong bottleneck (that results in a 10x decrease in $Ne$) that happens as recently as five generations ago (see fig 1a). Whereas we observe a loss in sample heterozygosity (fig 1b), it is small compared with the rapid increase in LD (fig 1c).

Fig 1d and fig1e show the relative change (compared with the values from a non-affected population) as the bottleneck happens more and more generations ago. Hopefully, one can see that sample heterozygosity is not informative of the recent past whereas LD is not informative of the ancient past. XXX.



\subsection{Analytical predictions}

In terms of coalescence, the effective population size can be defined in terms of the inverse of the instantaneous coalescence rate, that we denote simply as $f(t)$. We then define

\begin{equation}
	N_e(t) = \frac{1}{2f(t)}
\end{equation}

We need efficient analytical predictions for inference. For the case of $\pi$, it is well-known that

\begin{equation}
	\mathbb E[\pi \mid \theta] = 4 \mu \int_0^\infty t f(t)\,dt = 2 \mu \mathbb E[N_e]
\end{equation}


where $\mu$ is the mutation rate. From this equation it is apparent why the $\pi$ is weakly informative of recent $N_e$, as its value depends on $\mathbb E[N_e]$, that is, the mean value from 0 to infinity, which will most likely be dominated by the long tail.

The summary statistic $\mathbb E[X_iX_jY_iY_j]$, instead, has no known analytical prediction. Still, one can always obtain an approximate but unbiased estimate via Monte Carlo simulations:


XXX: add classical equation.

Although unbiased, this estimator is expensive. \citet{Fournier_2023} found the approximation expression and made an elegant link between long-range LD and IBD-sharing. They suggested that pairs of high-frequency loci (with minor allele frequency greater than 0.25) that are not very close ($u > 0.5 cM$) satisfy that $\mathbb{ E}[X_iX_j \mid\text{IBD}] \approx \mathbb{ E}[X^2] = 1$ and $\mathbb{ E}[X_iX_j \mid\neg\text{IBD}] \approx \mathbb{ E}[X]^2 = 0$. Under these assumptions, it follows that

\begin{align}
	\mathbb{E}[X_{i}Y_{i}X_{j}Y_{j}]
	 & = S(u)\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \text{IBD}]
	+ (1 - S(u))\, \mathbb{E}[X_{i}Y_{i}X_{j}Y_{j} \mid \neg\text{IBD}] = S(u)
\end{align}

where $S(u)$ is the probability that two loci that are $u$ Morgans apart are in IBD. For the case of diploid (unphased) individuals, \citet{Fournier_2023} proposed to neglect correlations induced by more than one pair of haplotypes being in IBD and suggested that
\begin{align}
	\mathbb{E}[G_{x,i} G_{y,i} G_{x,j} G_{y,j}]
	 & = \mathbb{E}[(X_{i,1}+X_{i,2})(Y_{i,1}+Y_{i,2})(X_{j,1}+X_{j,2})(Y_{j,1}+Y_{j,2})] \\
	 & = \frac{1}{4} \sum_{\alpha,\beta,\gamma,\delta = \{1,2\}}
	\mathbb{E}[X_{i,\alpha} X_{j,\beta} Y_{i,\gamma} Y_{j,\delta}]                        \\
	 & \approx \frac{1}{4} \sum_{\alpha,\beta = \{1,2\}}
	\mathbb{E}[X_{i,\alpha} X_{j,\beta} Y_{i,\alpha} Y_{j,\beta}]                         \\
	 & \approx \frac{1}{4} \sum_{\alpha,\beta = \{1,2\}} S(u) = S(u)
\end{align}

where $G_{x,i}$ and $G_{y,i}$ are the centred and standardised unphased diploid genotypes.

\citet{Fournier_2023}'s proxy, although approximate, gives an intuition of why linkage disequilibrium is informative of recent demography, as, essentially:

\begin{equation}
	\mathbb E[\mathbb E[X_iX_jY_iY_j]] \approx \int_0^\infty \exp(-2ty) f(t)\,dt
\end{equation}

which can be interpreted as assigning weights to each time in the past that decays exponentially.


\subsection{Biased but accurate predictor}

When predicting $E[X_i X_j Y_i Y_j]$ from a Wright-Fisher population with equation X, we may decompose the error in two sources. First, the error introduced in computing $\int_0^\infty \exp(-2ty) f(t)\,dt$ under the SMC' or SMC to obtain analytical predictions. Second, the "intrinsic" error of the approximation when considering that we use $\int_0^\infty \exp(-2ty) f(t)\,dt$ as a proxy for $E[X_iX_jY_iY_j]$.

We found that the first source of error has relatively low impact across a wide range of populations (see XXX). Interestingly, we found the bias introduced by the second error decreases as $N_e$ grows.

\begin{figure}[!ht]
	\centering
	% --- Row 1: Piecewise constant ---
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_piecewise_skeleton.pdf}
		\caption{Piecewise-constant demography.}
		\label{fig:bias:pw_skeleton}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_piecewise_small.pdf}
		\caption{Piecewise constant, $x = 1$.}
		\label{fig:bias:pw_small}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_piecewise_large.pdf}
		\caption{Piecewise constant, $x = 10$.}
		\label{fig:bias:pw_large}
	\end{subfigure}

	\vspace{0.5em}

	% --- Row 2: Exponential ---
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_exponential_skeleton.pdf}
		\caption{Piecewise exponential demography.}
		\label{fig:bias:exp_skeleton}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_exponential_small.pdf}
		\caption{Piecewise exponential, $x = 1$.}
		\label{fig:bias:exp_small}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.32\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/example_bias/example_bias_exponential_large.pdf}
		\caption{Piecewise exponential, $x = 10$.}
		\label{fig:bias:exp_large}
	\end{subfigure}
	\caption{Deterministic predictions vs.\ Monte Carlo simulations (SMC'/SMC and DTWF) for expected LD under two demographic models scaled by a factor $x$. (a)~Piecewise-constant skeleton. (b\textsubscript{a})~Small piecewise constant. (b\textsubscript{c})~Large piecewise constant. (c)~Piecewise-exponential skeleton. (d\textsubscript{a})~Small piecewise exponential. (d\textsubscript{c})~Large piecewise exponential.}
	\label{fig:bias}
\end{figure}

\bibliography{references}

\clearpage
\appendix
\section{Stdpopsim catalogue}

XXX.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.48\textwidth]{figures/stdpopsim/stdpopsim_caeele.pdf}
	\caption{Deterministic $\widehat{\mathbb E}_{\mathrm{F}}[X_iX_jY_iY_j]$ predictions vs.\ \texttt{stdpopsim} simulations for \textit{Caenorhabditis elegans}. Simulations were generated with \texttt{msprime} using a 100-generation discrete-time Wright-Fisher phase followed by the standard coalescent. Solid lines and shaded areas correspond to the observed mean and its 95\% bootstrap confidence interval. Dashed lines correspond to predictions under the SMC$'$ approximation using the true demographic parameters of each simulation. Notice that the binning scheme is much smaller than the minimum genetic distance of 0.5~cM suggested by \citet{Fournier_2023}, leading to a more noticeable bias.}
	\label{fig:stdpopsim:caeele}
\end{figure}

\end{document}
